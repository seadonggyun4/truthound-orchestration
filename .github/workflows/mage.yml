name: Mage

on:
  workflow_call:
    inputs:
      lane:
        required: false
        type: string
        default: pr
  workflow_dispatch:
    inputs:
      lane:
        description: CI lane to emulate
        required: true
        default: pr
        type: choice
        options:
          - pr
          - main
          - release
          - nightly

concurrency:
  group: mage-${{ github.event.pull_request.number || github.ref }}-${{ inputs.lane || github.event.inputs.lane || 'manual' }}
  cancel-in-progress: true

jobs:
  matrix:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    outputs:
      python_version: ${{ steps.export.outputs.python_version }}
      truthound_range: ${{ steps.export.outputs.truthound_range }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - id: export
        name: Export support matrix
        run: |
          python scripts/ci/export_support_matrix.py workflow \
            --workflow mage \
            --lane "${{ inputs.lane || github.event.inputs.lane || 'pr' }}" \
            --github-output "$GITHUB_OUTPUT"

  mage:
    needs: matrix
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ needs.matrix.outputs.python_version }}
          cache: pip
          cache-dependency-path: |
            pyproject.toml
            ci/support-matrix.toml
      - name: Install Mage dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev,mage,opentelemetry]" "truthound${{ needs.matrix.outputs.truthound_range }}"
      - name: Run Mage suites
        run: |
          set -euo pipefail
          mkdir -p artifacts/mage
          python -m pytest -q \
            packages/mage/tests \
            --junitxml="artifacts/mage/junit.xml" \
            --cov=common \
            --cov=packages/mage/src/truthound_mage \
            --cov-report="xml:artifacts/mage/coverage.xml" | tee artifacts/mage/pytest.log
      - name: Upload Mage artifacts
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: truthound-mage
          path: artifacts/mage/*

  summary:
    if: always()
    needs:
      - mage
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - name: Validate Mage results
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
        run: |
          python - <<'PY'
          import json
          import os
          needs = json.loads(os.environ["NEEDS_JSON"])
          failures = {
              name: details["result"]
              for name, details in needs.items()
              if details["result"] not in {"success", "skipped"}
          }
          if failures:
              raise SystemExit(f"Mage CI failed: {failures}")
          print("Mage summary passed")
          PY
