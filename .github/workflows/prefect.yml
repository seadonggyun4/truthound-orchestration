name: Prefect

on:
  workflow_call:
    inputs:
      lane:
        required: false
        type: string
        default: pr
  workflow_dispatch:
    inputs:
      lane:
        description: CI lane to emulate
        required: true
        default: pr
        type: choice
        options:
          - pr
          - main
          - release
          - nightly

concurrency:
  group: prefect-${{ github.event.pull_request.number || github.ref }}-${{ inputs.lane || github.event.inputs.lane || 'manual' }}
  cancel-in-progress: true

jobs:
  matrix:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    outputs:
      truthound_range: ${{ steps.export.outputs.truthound_range }}
      host_matrix: ${{ steps.export.outputs.host_matrix }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - id: export
        name: Export support matrix
        run: |
          python scripts/ci/export_support_matrix.py workflow \
            --workflow prefect \
            --lane "${{ inputs.lane || github.event.inputs.lane || 'pr' }}" \
            --github-output "$GITHUB_OUTPUT"

  prefect:
    needs: matrix
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.matrix.outputs.host_matrix) }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python_version }}
          cache: pip
          cache-dependency-path: |
            pyproject.toml
            ci/support-matrix.toml
      - name: Install Prefect dependencies
        run: |
          python -m pip install --upgrade pip
          mkdir -p artifacts/prefect/generated
          python - <<'PY'
          import json
          from pathlib import Path

          constraints = json.loads(r'''${{ toJSON(matrix.constraints) }}''')
          constraint_urls = json.loads(r'''${{ toJSON(matrix.constraint_urls) }}''')
          output_dir = Path("artifacts/prefect/generated")
          output_dir.mkdir(parents=True, exist_ok=True)
          (output_dir / "constraints.txt").write_text(
              "\n".join(constraints) + ("\n" if constraints else ""),
              encoding="utf-8",
          )
          (output_dir / "constraint-urls.txt").write_text(
              "\n".join(constraint_urls) + ("\n" if constraint_urls else ""),
              encoding="utf-8",
          )
          PY
          install_args=()
          if [ -s artifacts/prefect/generated/constraint-urls.txt ]; then
            while IFS= read -r constraint_url; do
              [ -n "${constraint_url}" ] || continue
              install_args+=(--constraint "${constraint_url}")
            done < artifacts/prefect/generated/constraint-urls.txt
          fi
          if [ -s artifacts/prefect/generated/constraints.txt ]; then
            install_args+=(-c artifacts/prefect/generated/constraints.txt)
          fi
          python -m pip install "${install_args[@]}" "prefect==${{ matrix.version }}"
          python -m pip install "${install_args[@]}" -e ".[prefect]" "truthound${{ needs.matrix.outputs.truthound_range }}"
          python -m pip install pytest pytest-cov pytest-asyncio pytest-xdist
      - name: Run Prefect suites
        run: |
          set -euo pipefail
          mkdir -p artifacts/prefect
          python -m pytest -q \
            packages/prefect/tests/test_blocks.py \
            packages/prefect/tests/test_tasks.py \
            packages/prefect/tests/test_flows.py \
            packages/prefect/tests/test_utils.py \
            --junitxml="artifacts/prefect/junit-${{ matrix.version }}.xml" \
            --cov=common \
            --cov=packages/prefect/src/truthound_prefect \
            --cov-report="xml:artifacts/prefect/coverage-${{ matrix.version }}.xml" | tee "artifacts/prefect/pytest-${{ matrix.version }}.log"
      - name: Upload Prefect artifacts
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: truthound-prefect-${{ matrix.version }}
          path: artifacts/prefect/*

  summary:
    if: always()
    needs:
      - prefect
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - name: Validate Prefect results
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
        run: |
          python - <<'PY'
          import json
          import os
          needs = json.loads(os.environ["NEEDS_JSON"])
          failures = {
              name: details["result"]
              for name, details in needs.items()
              if details["result"] not in {"success", "skipped"}
          }
          if failures:
              raise SystemExit(f"Prefect CI failed: {failures}")
          print("Prefect summary passed")
          PY
